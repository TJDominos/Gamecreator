export interface UserProfileInfo {
  avatarUrl: string;
  username: string;
  isVerified: boolean;
  hasStake: boolean;
  lastActive: string;
  bio: string;
  location: string;
  joinedDate: string;
}
